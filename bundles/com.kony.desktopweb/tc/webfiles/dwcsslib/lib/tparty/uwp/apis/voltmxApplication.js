if (window && window.Windows && Windows && Windows.UI && Windows.UI.Popups) {
    voltmx.application = voltmx.application || {};
    voltmx.application.setAppHeaders = function (params) {
        // Dummy function 
    };
    voltmx.application.setAppFooters = function (params) {
        // Dummy function
    };
}